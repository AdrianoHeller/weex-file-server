version: '3.1'

services:
  weex-ftp:
    build:
      context: .
      dockerfile: Dockerfile
    image: weex-ftp
    container_name: weex-ftp  
    expose:
      - "3001:3001"
    env_file: ./.env
    restart: unless-stopped
volumes:
  weex-ftp:      